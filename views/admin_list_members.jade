extends admin

block content
  table.table.table-striped
    thead
      tr
        th Email
        th Password
    tbody
    each user in users
      tr
        td #{user.email} 
        td #{user.password}