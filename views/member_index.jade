extends main

block title
  注册

block content
  if user != null
    欢迎#{user.email}
  else
    h3 会员登录
    fieldset
      form#login-form(action='/member/login', method='post')
        div
          label(for='email') 会员名
          input#email(name='email')
        div
          label(for='email') 密码
          input#password(name='password')
        div
        input(type='submit', value='登录')
        div
          p 还不是会员？
            a(href='/member/register')点此
            注册