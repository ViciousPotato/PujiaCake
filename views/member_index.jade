extends main

block title
  if user != null
    会员空间
  else
    登录

block sidebar
  if user != null
    style
      .welcome {
        font-size: 14px;
        mergin-bottom: 10px;
      }
    nav
      ul#sidenav.coral
        li
          a(href="/member/profile") 我的资料
        li
          a(href="/member/address") 我的地址
        li
          a(href="/member/orders") 我的订单
      p(style="margin-right: 25px;")
        a(href="/member/logout")注销
  else
    style
      #login-form {
        font-size: 13px;
      }

      #login-form .loginlabel {
        width: 90px;
        text-align: right;
        float: left;
        height: 26px;
        line-height: 26px;
      }

      #login-form input {
        width: 155px;
        height: 20px;
        margin-bottom: 3px;
      }

      #login-form .loginsubmit {
        height: 26px;
        border: 1px solid rgb(206, 28, 41);
        background-color: rgb(206, 28, 41);
        color: white;
        font-size: 13px;
      }

      #login-form .loginextra p {
        font-size: 10px;
        margin-bottom: 0px;
        margin-top: 0px;
        color: gray;
      }

    form#login-form(action='/member/login', method='post')
      div
        label.loginlabel(for='email') 会员名
        input#email.normal-input(name='email')
      div
        label.loginlabel(for='password') 密码
        input#password(name='password', type="password")
      div
      input.loginsubmit(type='submit', value='登录')
      div.loginextra
        p 
          a(href='/member/lostpass') 忘记密码？
          a(href='/member/lostpass') 点击
          找回
      div.otherlogin

    hr
    #register-notice
      p 还不是会员？
        a(href='/member/register')点击注册


block content
  style
    p { text-indent: 2em; }
    .boldtitle { font-weight: bold; text-indent: none; }
  
  div(style="height: 400px;")
    h3 成为会员
    p 当您成功购买商品后，系统会根据您购买的金额与商品属性，自动分配积分到您的账户中。
    p 对于获得积分的顾客，可以使用积分进行兑换商品、抵扣价格、升级会员等。
    p 具体获得与使用办法，参考
      a(href="/guide") “购物指南”
      | 下
      b 《积分获得与使用》。
    h3(style="margin-top: 20px; margin-bottom: 10px;") 会员特权
    ul
      li 商品打折服务
      li 最新的产品通告