extends member_index

block content
  h4 找回密码
  hr
    if (typeof success !== 'undefined')
      p 请查收邮件
    else
      form(action="/member/lostpass", method="post")
        label(for="email") 注册邮箱
        input#mail(name="email")

        input(type="submit", value="提交")